
<h1><%= @job.title %></h1>

<p>
  <%= simple_format(@job.description)%>
  <%= simple_format(@job.contact_email)%>
  <%= @job.wage_lower_bound %> <br>
  <%= @job.wage_upper_bound %>
</p>

<div class="text-center">
  <%= link_to("Upload your resume", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration:underline;")%>
</div>
